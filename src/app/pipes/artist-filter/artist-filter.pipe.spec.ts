import { ArtistFilterPipe } from './artist-filter.pipe';

describe('ArtistFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
